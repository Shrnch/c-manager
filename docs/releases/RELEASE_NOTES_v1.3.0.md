# c-manager v1.3.0

This release makes Result naming part of the creation flow.

After a Result is created, c-manager immediately opens a compact naming dialog.

The user can either:

- enter and save a custom Result name; or
- choose **Skip** to keep the normal generated Result name.

Naming remains optional, and the existing pencil action can still rename a Result later.
